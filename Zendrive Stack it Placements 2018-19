#include<bits/stdc++.h>
using namespace std;
typedef long long int64;
int main(){
    int n; cin >> n;
    vector<int>A(n);
    for(int i = 0; i < n; i++){
        cin >> A[i];
    }
    int x = 0;
    for(int i = 0; i < n; i++){
        x = __gcd(x, A[i]);
    }
    cout << x;
    return 0;
}
